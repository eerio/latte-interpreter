# DO TEGO PROGRAMU TRZEBA PODAĆ PARAMETR NA STDIN!
int fib(int x) {
    if (x <= 1) { return x; }
    else { return fib(x - 1) + fib(x - 2); }
}

int main() {
    int v = atoi(arg);
    v = v + 1;
    printInt(fib(v));

    return 0;
}

main();