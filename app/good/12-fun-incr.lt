int(int x) get_fun() {
  int foo(int x) {
    print("Calculating: 7 * x + 1 = ?? for x=");
    printInt(x);
    print(": ");
    return 7 * x + 1;
  }

  return foo;
}

int main() {
  int(int x) my_fun = get_fun();

  printInt(my_fun(7));

  return 17;
}

main();
